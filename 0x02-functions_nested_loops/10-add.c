#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @x: The first integer
 * @y: The second integer
 *
 * Return: The result which is sum of the two integers
 */
int add(int x, int y)
{
	return (x + y);
}
